'use strict';

describe('rememberAll.wordPractice module', function() {

  beforeEach(module('rememberAll.wordPractice'));

  describe('wordPractice controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var wordPracticeCtrl = $controller('wordPracticeController');
      expect(wordPracticeCtrl).toBeDefined();
    }));

  });
});