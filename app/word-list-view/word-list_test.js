'use strict';

describe('rememberAll.wordList module', function() {

  beforeEach(module('rememberAll.wordList'));

  describe('wordList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var wordListCtrl = $controller('wordListController');
      expect(wordListCtrl).toBeDefined();
    }));

  });
});